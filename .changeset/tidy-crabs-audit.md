---
'vouchington-tooling': minor
---

Add a repository-neutral dependency-license policy library that collects every platform represented
in a pnpm lockfile through an isolated, script-free audit workspace, validates pnpm's JSON report,
evaluates SPDX expressions with fail-closed semantics, and returns structured policy violations.
