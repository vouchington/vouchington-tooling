---
'vouchington-tooling': patch
---

Add a review-response disposition ladder to `agent-workflow` (invalid / blocking / non-blocking /
escalate) so agents give every review comment a recorded disposition and drive it to closure
instead of absorbing every suggestion or filing an issue per comment.
