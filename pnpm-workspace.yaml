packages:
  - packages/*
allowBuilds:
  '@ast-grep/cli': true
  '@launchql/protobufjs': true
# Dependabot already holds updates for a week. Do not also fail CI when the
# lockfile pins a toolchain release younger than pnpm's default 1-day cutoff.
minimumReleaseAge: 0
